# Replace with your configuration after you deploy
AWS_PROJECT_REGION=
AWS_COGNITO_REGION=
AWS_USER_POOLS_ID=
AWS_USER_POOLS_WEB_CLIENT_ID=
API_DISTRIBUTION_DOMAIN_NAME=
RAG_ENABLED=
DEFAULT_EMBEDDINGS_MODEL=
DEFAULT_CROSS_ENCODER_MODEL=
# Optional: comma-separated language codes to offer in the UI (en,es,zh,vi,ar).
# Leave blank to use the per-environment default (all languages on dev/staging,
# all except Arabic on prod).
ENABLED_LANGUAGES=